#!/usr/bin/env bash
# @generated by expo-modules-autolinking

set -eo pipefail

function with_node() {
  # Start with a default
  export NODE_BINARY=$(command -v node)

  # Override the default with the global environment
  ENV_PATH="$PODS_ROOT/../.xcode.env"
  if [[ -f "$ENV_PATH" ]]; then
    source "$ENV_PATH"
  fi

  # Override the global with the local environment
  LOCAL_ENV_PATH="${ENV_PATH}.local"
  if [[ -f "$LOCAL_ENV_PATH" ]]; then
    source "$LOCAL_ENV_PATH"
  fi

  if [[ -n "$NODE_BINARY" && -x "$NODE_BINARY" ]]; then
    echo "Node found at: ${NODE_BINARY}"
  else
    cat >&2 << NODE_NOT_FOUND
error: Could not find "node" executable while running an Xcode build script.
You need to specify the path to your Node.js executable by defining an environment variable named NODE_BINARY in your project's .xcode.env or .xcode.env.local file.
You can set this up quickly by running:

echo "export NODE_BINARY=\$(command -v node)" >> .xcode.env

in the ios folder of your project.
NODE_NOT_FOUND

    exit 1
  fi

  # Execute argument, if present
  if [[ "$#" -gt 0 ]]; then
    "$NODE_BINARY" "$@"
  fi
}

with_node \
  --no-warnings \
  --eval "require('expo/bin/autolinking')" \
  expo-modules-autolinking \
  generate-modules-provider  \
  --target "/home/ubuntu/natively-dev/rnxbuild/fixtures/01-bare/ios/Pods/Target Support Files/Pods-01bare/ExpoModulesProvider.swift" \
  --entitlement "/home/ubuntu/natively-dev/rnxbuild/fixtures/01-bare/ios/01bare/01bare.entitlements" \
   \
  --podfile-properties-file-path "/home/ubuntu/natively-dev/rnxbuild/fixtures/01-bare/ios/Podfile.properties.json" \
  --platform "apple" \
  --packages "@expo/dom-webview" "expo" "expo-asset" "expo-constants" "expo-file-system" "expo-font" "expo-keep-awake"
